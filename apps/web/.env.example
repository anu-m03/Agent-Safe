# Backend API URL (default: http://localhost:4000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Base chain ID (default: 8453 = Base Mainnet)
NEXT_PUBLIC_BASE_CHAIN_ID=8453

# Contract addresses JSON (optional override — defaults from @agent-safe/shared)
# NEXT_PUBLIC_CONTRACT_ADDRESSES_JSON='{}'
