# ─── Server ─────────────────────────────────────────────
PORT=4000
NODE_ENV=development

# ─── QuickNode (RPC + Streams) ───────────────────────────
# Leave empty to use public Base RPC; set for production.
QUICKNODE_RPC_URL=

# ─── Kite AI ─────────────────────────────────────────────
# Optional. If unset, governance summarisation uses deterministic stubs.
KITE_BASE_URL=https://rpc-testnet.gokite.ai
KITE_API_KEY=

# ─── Snapshot / Governance ───────────────────────────────
# Blockade Labs — Skybox AI (optional)
BLOCKADE_API_KEY=

# Snapshot / Governance (optional)
SNAPSHOT_GRAPHQL_URL=https://hub.snapshot.org/graphql
NOUNS_SNAPSHOT_SPACE=nouns.eth
SNAPSHOT_SPACES=agentsafe.eth
GOVERNANCE_CACHE_TTL_MS=60000
GOVERNANCE_VETO_WINDOW_SECONDS=3600

# ─── Logs & Data ────────────────────────────────────────
# Directory for logs.jsonl, queuedVotes.json, scenes.json, etc.
LOG_STORE_PATH=.data

# ─── Execution (ERC-4337 on Base) ───────────────────────
# Path to deployment JSON, or use env overrides below.
DEPLOYMENT_PATH=
BASE_RPC_URL=https://mainnet.base.org
BUNDLER_RPC_URL=
ENTRY_POINT_ADDRESS=0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789
AGENT_SAFE_ACCOUNT=
# Signer for UserOp and (optional) provenance recordApproval.
SWARM_SIGNER_PRIVATE_KEY=
EXECUTION_SIGNER_PRIVATE_KEY=

# ─── Provenance (onchain approvals) ──────────────────────
# Set in deployments/base.json or here. Zero = Kite-only provenance.
PROVENANCE_REGISTRY_ADDRESS=
# Comma-separated agent TBA addresses (need ≥2 for threshold).
PROVENANCE_AGENT_TBAS=
PROVENANCE_SIGNER_PRIVATE_KEY=

# ─── x402 Paid Actions (Base USDC) ────────────────────────
# Operator wallet on Base that receives USDC. Set for production CDP flow.
X402_OPERATOR_WALLET_BASE=
# USDC on Base mainnet (default: Circle canonical).
X402_USDC_BASE_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
# Required amount per action (USDC 6 decimals, e.g. 10000 = 0.01 USDC).
X402_AMOUNT_PROPOSAL_SUMMARISE=10000
X402_AMOUNT_RISK_CLASSIFICATION=20000
X402_AMOUNT_TX_SIMULATION=5000
X402_AMOUNT_REQUEST_PROTECTION=25000
# Stub: set X402_PAYMENT_TX_HASH to simulate successful payment (else fallback).
X402_PAYMENT_TX_HASH=
X402_ENABLED=

# ─── Streams (QuickNode webhooks) ────────────────────────
STREAMS_MAX_EVENTS=100
STREAMS_MAX_ALERTS=50

# ─── Payments (x402 record cap) ─────────────────────────
PAYMENTS_MAX_RECORDS=200

# === ERC-8021 Builder Code (Base.dev) ===
# Register free at https://base.dev → Settings → Builder Code
BASE_BUILDER_CODE=agentsafe42   # ← change to your actual registered code after you register
